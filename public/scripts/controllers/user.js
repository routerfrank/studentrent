'use strict';

/**
 * @ngdoc function
 * @name rentApp.controller:UserCtrl
 * @description
 * # UserCtrl
 * Controller of the rentApp
 */
angular.module('rentApp')
  .controller('UserCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
